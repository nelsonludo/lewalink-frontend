const LoadingSmallButton = () => {
  return (
    <button
      type="button"
      className="rounded-md bg-indigo-300 px-3 py-2 text-sm font-semibold text-white shadow-sm  focus-visible:outline focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-indigo-600"
    >
      Loading...
    </button>
  );
};

export default LoadingSmallButton;
